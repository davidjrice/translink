<h2>Services</h2>

<p><%= link_to "JSON", params.merge(:format => 'json') %></p>

<p>The routes denoted by <strong>X</strong> is currently unknown stops</p>
<p>This data was originally listed as one of ["Both", "CityCentre", "Other", "Terminal"] in the original data</p>

<table>
  <thead>
    <tr>
      <th>Area Code</th>
      <th>Code</th>
      <th>Name</th>
      <th>No. of Routes</th>
      <th>Routes</th>
    </tr>
  </thead>
  <tbody>
    <%- @services.each do |s| -%>
      <tr>
        <td><%= s.area.code %></td>
        <td><%= s.code %></td>
        <td><%= s.name %></td>
        <td><%= s.routes.size %></td>
        <td><%= s.routes.map{ |s| s.code }.join(",") %></td>
      </tr>
    <%- end -%>
  </tbody>
</table>